import React from 'react';
import { useThemeUpdate } from '../App'; // Import useThemeUpdate from '../App'

const ThemeSwitcher = () => {
  const toggleTheme = useThemeUpdate(); // Use useThemeUpdate hook

  // Theme switcher component code
};

export default ThemeSwitcher;


